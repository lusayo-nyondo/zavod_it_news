[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
version = "0.0.1"
name = "zavod_it_news"
description = "Zavod IT News React project"

[tool.mypy]
check_untyped_defs = true
